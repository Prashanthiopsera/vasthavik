# Deployment Context — vasthavik

Generated by Opsera Code-to-Cloud Agent | Mar 31, 2026

## Parameters

| Field | Value |
|---|---|
| App Name | `vasthavik` |
| Environment | `dev` |
| AWS Region | `us-west-2` |
| Runtime | `python` (backend) + `nodejs` (frontend) |
| GitHub Repo | `Prashanthiopsera/vasthavik` |
| EKS Namespace | `vasthavik-dev` |
| ECR Backend | `{account}.dkr.ecr.us-west-2.amazonaws.com/vasthavik-backend` |
| ECR Frontend | `{account}.dkr.ecr.us-west-2.amazonaws.com/vasthavik-frontend` |
| App URL | `https://vasthavik-dev.agents.opsera-labs.com` |
| ArgoCD | `https://argocd.agents.opsera-labs.com` |

## Services

| Service | Port | K8s Service Type | Image |
|---|---|---|---|
| backend (Flask API) | 5001 | ClusterIP | `vasthavik-backend:{tag}` |
| frontend (React SPA) | 3000 | LoadBalancer | `vasthavik-frontend:{tag}` |
| db (PostgreSQL) | 5432 | **AWS RDS** (external) | managed |

## GitHub Secrets Required

Set these in `https://github.com/Prashanthiopsera/vasthavik/settings/secrets/actions`:

| Secret | Description |
|---|---|
| `AWS_ACCESS_KEY_ID` | IAM user with ECR push + EKS access |
| `AWS_SECRET_ACCESS_KEY` | Corresponding secret |
| `AWS_ACCOUNT_ID` | 12-digit AWS account ID |
| `SECRET_KEY` | Flask SECRET_KEY (64-char hex) |
| `DATABASE_URL` | `postgresql://user:pass@rds-endpoint:5432/vastavik` |
| `POSTGRES_PASSWORD` | RDS master password |

## Architecture

```
GitHub Push (main)
    │
    ▼
GitHub Actions (ci-cd-vasthavik.yaml)
    ├── Build backend Docker image → push to ECR (vasthavik-backend:{sha})
    ├── Build frontend Docker image → push to ECR (vasthavik-frontend:{sha})
    └── Update k8s/overlays/dev/kustomization.yaml image tags → git push
          │
          ▼
        ArgoCD (watches k8s/overlays/dev/)
          │
          ▼
        EKS Namespace: vasthavik-dev
          ├── backend Deployment (1 replica, Flask API)
          ├── frontend Deployment (1 replica, React/serve)
          └── Services (ClusterIP backend, LoadBalancer frontend)
```

## GitOps Layout

```
k8s/
├── base/
│   ├── backend/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── configmap.yaml
│   ├── frontend/
│   │   ├── deployment.yaml
│   │   └── service.yaml
│   └── kustomization.yaml
└── overlays/
    └── dev/
        ├── kustomization.yaml      ← image tags updated by CI
        └── backend-secret.yaml     ← env vars from K8s secrets

gitops/
└── argocd/
    └── application.yaml           ← ArgoCD app pointing to overlays/dev
```

apiVersion: v1
kind: Service
metadata:
  name: vasthavik-backend
  labels:
    app: vasthavik
    component: backend
spec:
  type: ClusterIP
  selector:
    app: vasthavik
    component: backend
  ports:
    - name: http
      port: 5001
      targetPort: 5001
      protocol: TCP
